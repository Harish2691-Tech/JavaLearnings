
Comparison operators :
- We are using operators to compare premitive values.
-  == this expression is called a boolean expression. compares 2 values and provide truw/false result
-  != inequality operator.
-  >
- >= jfhwef
- <
- <=
----xxxxx-------xxxxx------xx
Logical operators :

- If we want to combine multiple boolean expression, then we can use Logical operators.
- && -- If both are all conditions are true then it get succeed
- || -- If any on eof the condition is true, then it will return true and execute. of 1st one is true, jave will not consider the next conditions.
- ! --

-------xxxxxx-----xxxx------xxxxx-----

If statements : 

  int temp = 32;
        if (temp > 30) {
            System.out.println("It's a hot day");
            System.out.println("Drink water");
        } else if (temp > 20 && temp <= 30) {
            System.out.println("Beautiful day");
        }
           else {
            System.out.println("Cold day");
        }


Simplify if statements :

- if(income >100000)
-   hasEligible = true;
-  else
-    hasEligible = false;

// simplist way: 
hasEligible = (income >100000) ; 

-----xxxxx-------xxxx-------xxxx

Ternary operators : 


