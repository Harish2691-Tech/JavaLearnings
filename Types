Types :
-- int - to store numbers

Primitive and References Types:

primitive - to store simple values
Reference (Non primitive types) - to store complex objects

Type  Bytes  Range
byte  1      -128 to 127
short  2    -32k to 32k
int   4      -2b to 2b   (upto 2 billion)
long   8
float  4
double  8
char    2      A,b,c,d
boolean  1    true / false


------

int accepts upto 2 billion. so for larger numbers we can use long. we need to use L suffix for the long data

long viewsCount = 4326634467L;
long viewsCount = 4_326_634_467L;

For decimal values use float and double.

float price = 10.22; // getting error
in java a number with decimal is considerd as double. to mention it is float, append suffix with F.

float price = 10.22F;


char with single quotes.

char letter = 'A';


string is sequence of characters.

 boolean isEligible = true / false; // allocation memory is 1 byte.


For primitive type memories allocated and release by Java Run Time environment. 
primitive types dont have members.

Example :
 public static void main(String[] args) {
        System.out.println("Hello world");
        int age=30, temp=20,yaxis=50;
        age= 35;
        int viewsCount = 1_234_567_890;
        byte bb = 127;
        boolean isEligible = true;
        System.out.println(age);
    }
--------------

Reference type : (Non primitive type):

For reference type, we have to allocate the memory. but do nt need to release the memory. JRE will take care of it.
reference type have members. object with dot operator we can access the members.

Date now = new Date();
now.getTime(); // 
now is instance od Date() class. 

--------------------

Memory management in Primitive and reference types:

byte x=1;
byte y = x;

x and y are store separately and in different location. So completely independent of each other.

--

Point point1 = new Point(1,2);

        Point point2 = point1;


1st it allocates some memory to pointer object. Lets say the address of the memory location is 100.
Then its going to allocate memory to point2. here it will store the address of pointer1. Not the value.


allocate memory for the pointer object. P1 and P2 pointing the same memory address of the pointer object. 

Reference types= are copy by the references.
Primitive types = are copy by the values.

Strings:

string is reference type in java.

we can access string methods uisng . operator. 


Diff b/w parameters and arguments :
      message.replace(target:"!",replacement:"*")

target and replacement - are parameters.
! and * - are arguments.

toLowerCase()
toUpperCase()
trim()
replace()
---------------xxxxxxx-----------xxxxx-------------

Escape Sequence :

To use special characters like ",\ means we use to prefix with \ 
\"
\\
\n new line
\t tab


-------------------

Arrays :

 int[] numbers = new int[5];
        numbers[0]=1;
        numbers[1]=2;
        numbers[2]=3;
        System.out.println(Arrays.toString(numbers));

---
int[] numbers = {1,2,3,4,5};
Arrays.sort(numbers);

----

Multi dimentional array :

 int[][] multiArrays = new int[2][3];
        multiArrays[0][0] =1;

        System.out.println(Arrays.deepToString(multiArrays));

output : [[1, 0, 0], [0, 0, 0]]

----

 int[][] multiArraysDifferent = {{1,2,3},{4,5,6}};
    
System.out.println(Arrays.deepToString(multiArraysDifferent));

output : [[1, 2, 3], [4, 5, 6]]

----------xxxxxxxxx-----xxxxxx--------xxxxxxxx

Constants : 


final float Ri = 3.14F;

again if u try to change, Ri = 1; // it shows error. bcoz final keyword in java act as constant.

final variable → must be assigned only once
Cannot be reassigned later

----------------------

Arithmetic expressions :

10/3 => will give 3 only... not 3.33333 
bcoz in java the whole is divided by whole num means... it will give whole num only

we need to convert this to get the decimal value

(double)10 / (double)3 = 3.333333333

double division = (double)10 / (double)3 = 3.333333333 ;

-----
int x = 1;
int y = x++; // x is assigned to y then only increment is happening.
System.out.println(x);
System.out-println (y);

output :
x->2
y->1

----
Augumented assigmnet operators (compound assigmnet operators)
x = x+2;
x += 2;  // Augumented assigmnet operators


---- xxxxxxx-------xxxxxxx-----

Order od operations :
()
*\
+-

--------xxxx-----

casting / type conversion :

// Implicit casting
// byte > short › int › long > float > double
double x = 1.1;
double y = x + 2; // implict casting from 2 to 2.0
System.out-println (y);

-----

Explict casting
double x = 1.1;
int y = (int)x + 2;
System.out. println (y);
----
int i = 1
double digit = Double.parseDoube(i) +2.2 ;  // it parse 1 to 1.0

------xxxxxxx-------xxxxxxx---------xxxxxxx

Math class :

Math.floor(1.1)  // 1
Math.ceil(1.1)  // 2
Math.round(1.1) //1
Math.max(1,2)  //2
Math.random() // provide random number
int data = (int) Math.Round( Math.Random() *100) ;

-----xxxxx-------xxxxx---------xxxx
Formatting numbers :

Abstract class : Cant create instance or object for an abstract class.

NumberFormat currency = NumberFormat.getCurrencyInstance();

Why not new NumberFormat()?

👉 Answer:

It’s an abstract class
Uses factory method for flexibility, encapsulation, and locale-aware formatting

Example: 
String currency = NumberFormat.getCurrencyInstance().format(1000); // method chaining
        System.out.println(currency);
------













